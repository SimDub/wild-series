{% extends 'base.html.twig' %}
{% block title %} {% endblock %}

{% block body %}
    <div class="card">
        <div class="row">
            <div class="card-title col-12 col-md-6">
                <h1>{{ episode.season.program.title }}</h1>
                <div class="card-body">
                    <p>Episode {{ episode.number }} : {{ episode.title }}</p>
                    <div class="col-md-10 offset-md-0">
                        <p>Synopsis :</p>
                        <p>{{ episode.synopsis }}</p>
                        <a class="btn btn-primary" href="{{ path('wild_program')}}/{{ title }}" role="button">{{ episode.season.program.title }}</a>
                        <a class="btn btn-primary" href="{{ path('wild_season')}}/{{ episode.season.id }}" role="button">{{ episode.season.description}}</a>
                    </div>
                    <a class="col-md-10 offset-md-0" href="{{ path('wild_index') }}">
                        Retour à l'accueil
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
